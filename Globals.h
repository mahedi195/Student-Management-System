#include<bits/stdc++.h>
#ifndef GLOBALS_H
#define GLOBALS_H

#include "Student.h"
#include <vector>

extern vector<Student> students; // declared as extern
extern int nextId;                    // declared as extern

#endif

